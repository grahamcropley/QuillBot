# Environment Configuration
# This project uses service-specific environment files:
#
# - .env.opencode.{local|lab|production} - OpenCode server configuration
# - .env.web.{local|lab|production}      - Next.js web server configuration
#
# For local development:
#   1. Copy .env.opencode.example to .env.opencode.local
#   2. Copy .env.web.example to .env.web.local
#   3. Fill in your credentials
#
# See .env.opencode.example and .env.web.example for details.

# OpenCode Server Authentication (for "opencode serve" command)
# Set these when starting the OpenCode server
OPENCODE_SERVER_USERNAME=quillbot-opencode-api
OPENCODE_SERVER_PASSWORD=

# Client credentials (for Next.js API routes to authenticate with OpenCode)
OPENCODE_API_KEY=

# Azure OpenAI / Azure AI Foundry provider configuration
AZURE_RESOURCE_NAME=
AZURE_API_KEY=
# Deployment name must match selected Azure model
AZURE_MODEL=azure/gpt-5.2-chat
