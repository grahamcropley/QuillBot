# QuillBot Configuration
# Copy this file to .env.local and update values for your environment

# =============================================================================
# Authentication (Azure Easy Auth)
# =============================================================================
# In production: Azure Container Apps Easy Auth handles authentication.
# No configuration needed here - enable it in Azure Portal:
#   Container Apps > Your App > Authentication > Add Microsoft provider
#
# For local development: Set a mock user to simulate authentication
# Format: "email|Display Name"
EASY_AUTH_DEV_USER=developer@yourcompany.com|Local Developer

# =============================================================================
# OpenCode Configuration
# =============================================================================

# OpenCode API Base URL (the headless server endpoint)
OPENCODE_API_URL=http://localhost:9090

# OpenCode API Port (if separate from URL)
OPENCODE_API_PORT=9090

# API Key for authentication (if required by your OpenCode setup)
OPENCODE_API_KEY=your-api-key-here

# WebSocket URL for streaming responses (optional - defaults to ws://localhost:9090/ws)
OPENCODE_WS_URL=ws://localhost:9090/ws

# Project file storage path (where markdown files are saved)
PROJECT_STORAGE_PATH=./projects

# Session timeout in minutes
SESSION_TIMEOUT=60

# Enable debug logging
DEBUG_MODE=false
