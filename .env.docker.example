# ============================================================================
# Environment Variables for Docker Compose
# ============================================================================
# This file should be copied to .env and customized for your environment.
# Variables here are substituted into docker-compose.yml at runtime.
#
# Security Note: This .env file should be in .gitignore (it is).
# NEVER commit secrets to version control.
# ============================================================================

# ----------------------------------------------------------------------------
# OpenCode Image Configuration
# ----------------------------------------------------------------------------
# Override to use custom OpenCode image or version
OPENCODE_IMAGE=
OPENCODE_VERSION=1.1.56

# Override to use custom web image
WEB_IMAGE=

# ----------------------------------------------------------------------------
# OpenCode Server Authentication
# ----------------------------------------------------------------------------
# HTTP Basic Auth credentials for OpenCode server
# IMPORTANT: These MUST match between OpenCode server and web client
OPENCODE_SERVER_USERNAME=quillbot-opencode-api
OPENCODE_SERVER_PASSWORD=

# ----------------------------------------------------------------------------
# Azure OpenAI / Azure AI Foundry Configuration
# ----------------------------------------------------------------------------
# Required for LLM operations
AZURE_RESOURCE_NAME=
AZURE_API_KEY=

# ----------------------------------------------------------------------------
# Optional Features
# ----------------------------------------------------------------------------
# Enable EXA search integration (1 = enabled, 0 = disabled)
OPENCODE_ENABLE_EXA=1

# Development Easy Auth user simulation (format: email|display_name)
# Only used in local development, ignored in production
EASY_AUTH_DEV_USER=your.email@company.com|Your Name
