# Next.js Web Server - Local Development
# Copy this to .env.web.local and fill in your values

# OpenCode API connection (use localhost for local dev, service name for Docker)
OPENCODE_API_URL=http://localhost:9090

# HTTP Basic Auth credentials (must match OpenCode server credentials)
OPENCODE_SERVER_USERNAME=quillbot-opencode-api
OPENCODE_SERVER_PASSWORD=

# Development Easy Auth user (format: email|display_name)
EASY_AUTH_DEV_USER=your.email@company.com|Your Name

# Azure OpenAI credentials (if needed for client-side operations)
AZURE_RESOURCE_NAME=
AZURE_API_KEY=
