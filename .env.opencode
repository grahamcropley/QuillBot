# OpenCode Server - Production Environment (Azure Container Apps)

# HTTP Basic Auth credentials (server validates these)
OPENCODE_SERVER_USERNAME=
OPENCODE_SERVER_PASSWORD=

# Azure OpenAI / Azure AI Foundry provider
AZURE_RESOURCE_NAME=
AZURE_API_KEY=

# Enable EXA search integration
OPENCODE_ENABLE_EXA=1

# Production-specific paths (Azure Container Apps managed volumes)
DATA_PATH=/app/data
OPENCODE_CONFIG_PATH=/app/.config/opencode

# Container hostname/domain for production
OPENCODE_HOSTNAME=quillbot-opencode
OPENCODE_DOMAINNAME=

# Network (default for Azure Container Apps)
NETWORK_NAME=default
